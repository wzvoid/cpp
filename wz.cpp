#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

bool VerifySquenceOfBST(vector<int> &sequence, int left, int right) {
    if (sequence.empty())
        return false;
    int size = sequence.size();
    int i = 0;
    while ()
        return true;
}

int main() {

    return 0;
}